import type {VercelRequest,VercelResponse} from '@vercel/node'; import Stripe from 'stripe'; import {db} from '../_lib/db';
export const config={api:{bodyParser:false}};
async function raw(req:VercelRequest){const chunks:any[]=[];for await(const c of req)chunks.push(c);return Buffer.concat(chunks);}
export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=='POST')return res.status(405).end();try{const key=process.env.STRIPE_SECRET_KEY,secret=process.env.STRIPE_WEBHOOK_SECRET;if(!key||!secret)return res.status(503).end();const stripe=new Stripe(key);const event=stripe.webhooks.constructEvent(await raw(req),req.headers['stripe-signature'] as string,secret);const obj:any=event.data.object;const tenantId=obj.metadata?.tenantId;let status:string|undefined;if(event.type==='checkout.session.completed'){status='active';}if(event.type==='customer.subscription.updated'||event.type==='customer.subscription.created'){status=obj.status;}if(event.type==='customer.subscription.deleted'){status='canceled';}if(status&&tenantId){await db().query('UPDATE tenants SET subscription_status=$1,stripe_customer_id=COALESCE($2,stripe_customer_id),stripe_subscription_id=COALESCE($3,stripe_subscription_id),updated_at=now() WHERE id=$4',[status,obj.customer||null,obj.id||null,tenantId]);}return res.json({received:true});}catch(e:any){return res.status(400).json({error:e.message});}}
